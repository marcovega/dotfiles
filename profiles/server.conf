# Server Profile - Headless server setup
# No GUI applications, just essential server tools

PROFILE_NAME="Server"
PROFILE_DESCRIPTION="Headless server setup with essential tools only"

# Scripts to run
SCRIPTS=(
  "scripts/system/packages.sh"
  "scripts/system/generate-configs.sh"
  "scripts/shell/zsh-setup.sh"
)

# Configs to stow
CONFIGS=(
  "configs/shell-zsh"
  "configs/git"
  "configs/terminal"
)
