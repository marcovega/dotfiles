# Server Profile - Headless server setup
# No GUI applications, just essential server tools

PROFILE_NAME="Server"
PROFILE_DESCRIPTION="Headless server setup with essential tools and SSH/GPG keys"

# Scripts to run
SCRIPTS=(
  "scripts/system/packages.sh"
  "scripts/system/generate-configs.sh"
  "scripts/shell/zsh-setup.sh"
  "scripts/security/ssh-keys.sh"
  "scripts/security/gpg-keys.sh"
)

# Configs to stow
CONFIGS=(
  "stow/zsh"
  "stow/terminal"
)
