# Minimal Profile - Essential tools only
# Perfect for servers or minimal setups

PROFILE_NAME="Minimal"
PROFILE_DESCRIPTION="Essential shell, git tools, and SSH/GPG keys"

# Scripts to run
SCRIPTS=(
  "scripts/system/packages.sh"
  "scripts/system/generate-configs.sh"
  "scripts/shell/zsh-setup.sh"
  "scripts/security/ssh-keys.sh"
  "scripts/security/gpg-keys.sh"
)

# Configs to stow
CONFIGS=(
  "stow/zsh"
  "stow/terminal"
)
